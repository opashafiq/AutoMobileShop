I need a crud and as reference you can use:
src/app/(DashboardLayout)/react-tables/master/tax-id/page.tsx
src/app/components/react-tables/master/taxid-datatable/index.tsx

Api details are as follows:
Get:
https://localhost:44352/api/TaxRateModified
[
  {
    "id": 0,
    "tbtm_Note": "string",
    "userName": "string",
    "setDate": "2026-06-28T04:41:10.391Z"
  }
]
Get by Id:
https://localhost:44352/api/TaxRateModified/10
  {
    "id": 0,
    "tbtm_Note": "string",
    "userName": "string",
    "setDate": "2026-06-28T04:41:10.391Z"
  }

  
  
Create using Post:
https://localhost:44352/api/TaxRateModified
  {
    "id": 0,
    "tbtm_Note": "string",
    "userName": "string",
    "setDate": "2026-06-28T04:41:10.391Z"
  }

Update useng Put: https://localhost:44352/api/TaxRateModified/12  
  {
    "id": 0,
    "tbtm_Note": "string",
    "userName": "string",
    "setDate": "2026-06-28T04:41:10.391Z"
  }

Delete using Delete:
https://localhost:44352/api/TaxRateModified/12


Instructions:
1) Reference files are:
   src/app/(DashboardLayout)/react-tables/master/tax-id/page.tsx
   src/app/components/react-tables/master/taxid-datatable/index.tsx
2) If you search the reference files, you will see that BASE_URL can be added using: const API_URL = getApiUrl('/api/TaxRateModified')
3) No need to show the username and setdate in the datatable and in the create and edit form.
Column userName= logged in user name, you will get hint from the reference files.
   Column setDate= current PC time, you will get hint from the reference files.

4) Create and Edit will be popped up as in reference files.
5) For deletion, please check the reference files.
6) Model interface will be imported from a common file, please check the reference files.
7) Menu is in: src/app/(DashboardLayout)/layout/vertical/sidebar/Sidebaritems.tsx  
    Please create a link under heading: "Master Data".
8) Please provide the datatable and other variables appropriate name matching with "TaxRateModified".











=== CRUD CREATION INSTRUCTIONS ===
1. In the page.tsx file, do NOT add any <h1> title below the <BreadcrumbComp> — the breadcrumb already displays the title. Remove any redundant <h1> that repeats the breadcrumb.
2. In the datatable index.tsx component card title, use a meaningful verb-prefixed title instead of a generic one. E.g. "Manage <Entity>" (like "Manage Daily Expenses", "Manage Tax IDs", "Manage Application Users") rather than "<Entity> Table" or "<Entity> Records".
3. Refer to the latest versions of these files (after the fixes were applied) as the canonical pattern:
   - src/app/(DashboardLayout)/react-tables/master/application-user/page.tsx (no h1 after breadcrumb)
   - src/app/components/react-tables/master/applicationuser-datatable/index.tsx (card title: "Manage Application Users")
